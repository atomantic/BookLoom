TEAM_ID=TYQ32QCF6K
APPSTORE_API_KEY_ID=YOUR_KEY_ID
APPSTORE_ISSUER_ID=YOUR_ISSUER_ID
# Keep the .p8 in a local-only directory — NOT iCloud Drive/Dropbox/etc., which
# would sync this upload-capable signing key to every device on the account.
APPSTORE_API_PRIVATE_KEY_PATH=~/.private_keys/AuthKey_XXXXXXXXXX.p8
